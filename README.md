# kanab-dist

Distribution artifacts for the [Kanab](https://kanab.dev) desktop app.

Releases here hold compiled binary blobs the app downloads on first launch and
through its auto-updater. Each file is content-addressed by SHA-256. The app
itself fetches them; you don't need to interact with this repo directly.

Source code lives in a separate private repository.
